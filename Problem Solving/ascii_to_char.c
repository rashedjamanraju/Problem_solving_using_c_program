#include<stdio.h>
int main(){
    //ascii to character
    int a,b;
    a=65;
    b=97;
    printf("When ASCII value is = %d the caracter value will be = %c ,",a,a);
    printf("When ASCII value is = %d the caracter value will be = %c",b,b);
    return 0;
}